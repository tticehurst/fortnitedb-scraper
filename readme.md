### A project to find free v-bucks missions on fortnite using fortnitedb

Configuration:
  - Rename `.env.example` to `.env`
  - Supported platforms: ntfy and discord
  - Fill out the required fields in the example `.env` file.